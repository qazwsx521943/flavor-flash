
excluded:
  - Pods
disabled_rules:
  - trailing_whitespace
identifier_name:
  allowed_symbols: ["_"] # these are allowed
  excluded: # excluded via string array
    - id
    - URL
    - url
    - app
file_length:
  warning: 500
  error: 800
type_body_length:
  - 500 # warning
  - 700 # error
